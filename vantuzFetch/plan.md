fetch
macos version
uptime
device name
kernel version
cpu (w/ e and p core config) (--real-cpu-freq to show real cpu freq else using static pre-loaded data)
gpu (w/ cores count)
ram (w/ memory type & speed)
swap
disks (w/ fs, name, volume)

planned:
display
battery

